package com.test.vote.manager.rest.dto;

public enum BackendErrors {
    NONE,
    CONVERSION_ERROR,
    UNEXPECTED_ERROR,
    UNAUTHORISED_ERROR,
    NOT_FOUND_ERROR,
    ALREADY_PRESENT_ERROR,
    INAPPROPRIATE_TIME_ERROR,
    FORBIDDEN_ERROR
}
